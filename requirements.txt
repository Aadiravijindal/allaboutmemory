# core (needed to run everything)
flask>=3.0
pyyaml>=6.0
cryptography>=42.0
pytest>=8.0

# API + dashboard
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
httpx>=0.27          # FastAPI TestClient

# live connectors + SDK (HTTP)
requests>=2.31

# --- production only (uncomment / paste keys to activate) ---
# psycopg[binary]>=3.1    # Postgres + pgvector backend (DATABASE_URL)
# boto3>=1.34             # AWS KMS key provider (MV_KMS_PROVIDER=aws)
# openai>=1.0             # real embeddings (MV_EMBEDDINGS=openai)
# stripe>=8.0             # billing (STRIPE_SECRET_KEY)
# temporalio>=1.5         # optional: swap durable.py for real Temporal
