# core (needed to run everything)
flask>=3.0
pyyaml>=6.0
cryptography>=42.0
pytest>=8.0

# API + dashboard
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
httpx>=0.27          # FastAPI TestClient

# live connectors + SDK (HTTP)
requests>=2.31

# --- production only (not required for the demo) ---
# psycopg[binary]>=3.1    # Postgres + pgvector backend
# boto3>=1.34             # AWS KMS key provider
# openai>=1.0             # real embeddings / in-tenant transform
# temporalio>=1.5         # durable sync workflows
