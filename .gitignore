# vault databases & keys (customer data — never commit)
*.db
*.db.key
*.key

# API control-plane data (tenants, keys, audit)
data/

# connector delivery outboxes (generated at runtime)
fixtures/*.outbox.json

# python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# reports
*.report.json
